(function(){
window.FinUI={
 b:(t,c='info')=>`<span class="badge ${c}">${t}</span>`,
 m:v=>Financeiro.money(v),
 d:d=>Melo.fmtDate(d),
 modal(html){let el=document.querySelector('#modal');if(!el){el=document.body.appendChild(document.createElement('div'));el.id='modal'}el.className='modal';el.innerHTML=`<div class="modal-card panel"><div class="modal-head"><b>Melo Reparos</b><button class="btn small" data-close>Fechar</button></div>${html}</div>`;el.querySelector('[data-close]').onclick=()=>el.className='hidden';return el;},
 options(arr,val=''){return arr.map(x=>`<option value="${x.id||x}" ${(x.id||x)===val?'selected':''}>${x.nome||x.numero||x}</option>`).join('')},
 bindCritical(){document.querySelectorAll('[data-export]').forEach(b=>b.onclick=()=>Melo.toast('Exportação fictícia preparada em CSV/PDF.'));},
 periodControls(p){return `<section class="panel"><div class="filters"><div class="field"><label>Período</label><select id="periodo"><option value="hoje">hoje</option><option value="semana">esta semana</option><option value="mes" selected>este mês</option><option value="mes-anterior">mês anterior</option><option value="proximo-mes">próximo mês</option><option value="custom">período personalizado</option></select></div><div class="field"><label>Data inicial</label><input type="date" id="ini" value="${p.ini||''}"></div><div class="field"><label>Data final</label><input type="date" id="fim" value="${p.fim||''}"></div><div class="field"><label>&nbsp;</label><button class="btn primary" id="aplicarPeriodo">Aplicar</button></div><div class="field"><label>&nbsp;</label><button class="btn" id="limparPeriodo">Limpar</button></div></div></section>`},
 contaReceberModal(state,onDone){const f=state.financeiro;const el=this.modal(`<h2>Nova conta a receber</h2><form id="formCR"><div class="detail-grid"><div class="field"><label>Origem</label><select name="origem"><option>OS</option><option>complemento</option><option>lançamento manual</option><option>ajuste</option><option>outro</option></select></div><div class="field"><label>OS</label><select name="osId"><option value="">Sem OS</option>${state.os.map(o=>`<option value="${o.id}" ${o.id==='os-1042'?'selected':''}>OS ${o.numero} · ${o.placa}</option>`).join('')}</select></div><div class="field"><label>Pagador</label><input name="pagador" value="Marina Lopes"></div><div class="field"><label>Tipo pagador</label><select name="tipoPagador"><option>cliente</option><option>seguradora</option><option>terceiro</option><option>empresa parceira</option></select></div><div class="field"><label>Descrição</label><input name="descricao" value="Lançamento manual revisado"></div><div class="field"><label>Valor bruto</label><input type="number" name="valorBruto" value="1000" step="0.01"></div><div class="field"><label>Desconto</label><input type="number" name="desconto" value="0" step="0.01"></div><div class="field"><label>Forma</label><select name="formaPagamento"><option>pix</option><option>débito</option><option selected>crédito</option><option>boleto</option><option>transferência</option></select></div><div class="field"><label>Parcelas</label><input type="number" name="parcelas" value="3" min="1"></div><div class="field"><label>Vencimento</label><input type="date" name="vencimento" value="${state.today}"></div><div class="field"><label>Regra</label><select name="regraPagamentoId">${this.options(f.regras)}</select></div><div class="field"><label>Responsável</label><input name="responsavel" value="Carla Supervisora"></div></div><p id="calcPrev" class="callout"></p><h3>Parcelas geradas visualmente</h3><div id="parcelasPrev" class="table-wrap"></div><textarea name="observacoes" placeholder="Observações"></textarea><button class="btn primary">Confirmar e criar conta fictícia</button></form>`);const form=el.querySelector('#formCR');const recalc=()=>{const data=Object.fromEntries(new FormData(form));const c=Financeiro.calcTax(f,Number(data.valorBruto||0)-Number(data.desconto||0),data.formaPagamento,Number(data.parcelas||1));el.querySelector('#calcPrev').innerHTML=`Revisão antes de confirmar: taxa ${FinUI.m(c.taxa)} · líquido ${FinUI.m(c.liquido)} · fórmula ${c.formula}. Ajuste manual exige justificativa.`;let rows='';for(let i=1;i<=Number(data.parcelas||1);i++){rows+=`<tr><td>${i}/${data.parcelas}</td><td><input value="${(Number(data.valorBruto||0)/Number(data.parcelas||1)).toFixed(2)}"></td><td><input type="date" value="${data.vencimento}"></td><td><button type="button" class="btn small">redistribuir diferença</button></td></tr>`}el.querySelector('#parcelasPrev').innerHTML=`<table class="table"><tbody>${rows}</tbody></table>`};form.oninput=recalc;recalc();form.onsubmit=e=>{e.preventDefault();Financeiro.createContaReceber(state,Object.fromEntries(new FormData(form)));Melo.toast('Conta a receber criada, parcelas geradas e OS atualizada.');el.className='hidden';onDone&&onDone();};},
 contaPagarModal(state,onDone){const f=state.financeiro;const el=this.modal(`<h2>Nova conta a pagar</h2><form id="formCP"><div class="detail-grid"><div class="field"><label>Origem</label><select name="origem"><option>compra</option><option>custo fixo</option><option>serviço terceirizado</option><option>material</option><option>imposto</option><option>folha</option><option>lançamento manual</option><option>outro</option></select></div><div class="field"><label>Fornecedor</label><input name="fornecedor" value="Auto Peças Central"></div><div class="field"><label>Descrição</label><input name="descricao" value="Conta criada a partir de compra"></div><div class="field"><label>Compra relacionada</label><select name="compraId"><option value="">Sem compra</option>${(state.compras||[]).map(c=>`<option value="${c.id}">${c.numero} · ${c.fornecedor}</option>`).join('')}</select></div><div class="field"><label>OS relacionada</label><select name="osId"><option value="">Sem OS</option>${state.os.map(o=>`<option value="${o.id}" ${o.id==='os-1042'?'selected':''}>OS ${o.numero}</option>`).join('')}</select></div><div class="field"><label>Categoria</label><select name="categoria">${f.categorias.filter(c=>c.tipo==='despesa').map(c=>`<option>${c.nome}</option>`).join('')}</select></div><div class="field"><label>Valor</label><input type="number" name="valor" value="1200" step="0.01"></div><div class="field"><label>Desconto</label><input type="number" name="desconto" value="0"></div><div class="field"><label>Juros</label><input type="number" name="juros" value="0"></div><div class="field"><label>Vencimento</label><input type="date" name="vencimento" value="${state.today}"></div><div class="field"><label>Forma</label><select name="formaPagamento"><option>boleto</option><option>pix</option><option>transferência</option><option>cartão</option></select></div><div class="field"><label>Parcelas</label><input type="number" name="parcelas" value="1" min="1"></div></div><p class="callout">Compras não geram obrigação automaticamente: este modal representa a confirmação explícita.</p><textarea name="observacao" placeholder="Observação"></textarea><button class="btn primary">Confirmar conta a pagar</button></form>`);el.querySelector('form').onsubmit=e=>{e.preventDefault();Financeiro.createContaPagar(state,Object.fromEntries(new FormData(e.target)));Melo.toast('Conta a pagar criada e compra/OS vinculadas.');el.className='hidden';onDone&&onDone();};},
 baixaModal(state,tipo,onDone){const f=state.financeiro;const contas=tipo==='receber'?f.contasReceber:f.contasPagar;const el=this.modal(`<h2>${tipo==='receber'?'Registrar recebimento':'Registrar pagamento'}</h2><form id="baixa"><div class="detail-grid"><div class="field"><label>Conta</label><select name="contaId" id="contaSel">${contas.map(c=>`<option value="${c.id}">${c.id} · ${c.pagador||c.fornecedor} · saldo ${FinUI.m(c.saldo)}</option>`).join('')}</select></div><div class="field"><label>Parcela</label><select name="parcelaId" id="parcelaSel"></select></div><div class="field"><label>Data</label><input type="date" name="data" value="${state.today}"></div><div class="field"><label>Valor ${tipo==='receber'?'recebido':'pago'}</label><input type="number" name="${tipo==='receber'?'valor':'valorPago'}" value="100" step="0.01"></div><div class="field"><label>${tipo==='receber'?'Taxa real':'Juros'}</label><input type="number" name="${tipo==='receber'?'taxaReal':'juros'}" value="0" step="0.01"></div><div class="field"><label>${tipo==='receber'?'Valor líquido real':'Desconto'}</label><input type="number" name="${tipo==='receber'?'valorLiquidoReal':'desconto'}" value="100" step="0.01"></div><div class="field"><label>Forma</label><select name="formaPagamento"><option>pix</option><option>boleto</option><option>crédito</option><option>débito</option><option>transferência</option></select></div><div class="field"><label>Comprovante</label><input name="comprovante" value="comprovante-simulado.pdf"></div><div class="field"><label>Responsável</label><input name="responsavel" value="Carla Supervisora"></div></div><label><input type="checkbox" name="estorno"> Registrar como estorno/ajuste</label><textarea name="observacao" placeholder="Observação e justificativa para diferença"></textarea><p class="callout">Baixa menor que o saldo mantém status parcial; baixa maior exige justificativa simulada e fica no histórico.</p><button class="btn primary">Confirmar baixa</button></form>`);const sel=el.querySelector('#contaSel'), ps=el.querySelector('#parcelaSel');function fill(){const id=sel.value;const pars=(tipo==='receber'?f.parcelasReceber:f.parcelasPagar).filter(p=>p.contaId===id);ps.innerHTML=pars.map(p=>`<option value="${p.id}">${p.numero}/${p.total} · vence ${FinUI.d(p.vencimento)} · saldo ${FinUI.m(p.saldo)}</option>`).join('');}sel.onchange=fill;fill();el.querySelector('form').onsubmit=e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));data.estorno=e.target.estorno.checked;Financeiro.baixa(state,tipo,data);Melo.toast('Baixa registrada, saldo e histórico atualizados.');el.className='hidden';onDone&&onDone();};}
};
})();
